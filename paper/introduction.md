# Introduction

Unit testing is time-consuming, and LLMs may help generate unit tests automatically. However, the generated tests must be evaluated using objective software testing metrics such as branch coverage and mutation score.

This study evaluates whether GPT-4 generated unit tests can reach branch coverage >= 80% and mutation score >= 60%, compared with student-written manual unit tests.

